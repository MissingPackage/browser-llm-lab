# PHASES — engine-fase-c3c (paging in scarsità)

Decomposizione dal contratto v1 (riletto all'apertura 2026-08-08; pre-avvio
docket item 2 ESEGUITO alla chiusura C3b). Sequenziale: le fasi 3-7 toccano
tutte `src/engine/{residency,glmmodel}.ts` e la telemetria — nessun
parallel-group. Riferimenti di meccanismo: colibri §2 (tier.h/AUTOPIN), C1
(recall LOOKA 92.0% @K=8, matrice usage), WP-0 (Belady ceiling, collasso in
scarsità), c3b (decode ottimistico attivo, finding P(dirty)↔prefill).

| # | phase | done-when (mechanical) | authority delta | owns | status |
|---|-------|------------------------|-----------------|------|--------|
| 1 | WP banda fredda browser | tool committato in tools/opfs-cold/** che misura IN CHROME la lettura OPFS a page cache FREDDA su blocchi expert-size (5.33 MB); JSON in results/opfs-bench/ con p50/p95 random expert-size e sequenziale; confronto ESPLICITO nel JSON coi numeri OS di C1 (random 1.63 GB/s / 3.74 ms p50; seq 3.22 GB/s); ideas-ledger §A e direction §8.3 aggiornati col numero browser; freddezza della cache dimostrata dal protocollo nel JSON (drop/riavvio dichiarato), non asserita | none | tools/opfs-cold/, results/opfs-bench/, docs/engine/ideas-ledger.md, docs/engine/direction.md | DONE (it.1: rand expert cold 1.79-1.94 GB/s p50 2.9-3.1 ms, seq 3.43-3.73 — parità col bound OS) |
| 2 | Spec C3c (coi numeri del WP dentro) | docs/superpowers/specs/2026-08-XX-engine-fase-c3c-design.md esiste e fissa: budget slab = f(VRAM, residente non-expert, KV(ctx)); tier.h + AUTOPIN con pin ≤ 12.5% degli slot; prefetch IN-FORWARD (tap hidden L → router L+1) con esclusione ESPLICITA del predictor al confine (WP-0); definizione operativa di instant-on + strumento di misura; budget TTFT a freddo; registrazione a docket + PROPOSTA per docket item 1 (budget instant-on assoluto vs relativo 1.25×) depositata coi numeri per il ruling PI; se la spec tocca gate/soglie: STOP, ruling | none | docs/superpowers/specs/, .harness/goals/engine-fase-c3c/docket.md | DONE (it.2: spec 2026-08-08 depositata, docket item 4; proposta item 1 = 1-bis, raccomandata 1.25× auto-ancorato) |
| 3 | Slab ctx-aware | run glmbench a ctx 6k che NON va OOM, con report JSON committato che mostra budget slab CALCOLATO dalla formula della spec e VRAM di picco osservata; a ctx ~500 il budget calcolato riproduce il regime attuale (non-regressione: decode/TTFT in banda ±5% sul punto b12); suite + tsc puliti | none | src/engine/{residency,gpulimits,glmmodel}.ts, src/engine/glmbench/, tests/, results/engine/ | DONE (it.3: ctx6k no-OOM a budget calcolato 12.146 GiB; ctx525 auto = regime tetto 16.55/12.53; riserva tarata 512 MiB) |
| 4 | Prefetch in-forward + recall in-engine | prefetch dal tap hidden di L al router di L+1 dentro il token, dietro flag; recall lookahead misurato IN-ENGINE sul corpus di C1 e confrontato nel JSON col 92.0% @K=8 dell'oracolo (scostamento SPIEGATO nel journal, non gateato); identità del forward invariata (ktest esistenti PASS, argmax = sync sul campione); suite + tsc puliti | none | src/engine/{glmmodel,residency,telemetry}.ts, src/engine/kernels/, src/engine/ktest/, tests/, results/engine/ | DONE (it.4: recall@8 91.92% vs 92.0 oracolo; firma 14b esatta e cpuref 256/256 con prefetch ON) |
| 5 | Policy tier+AUTOPIN vs LRU | agli STESSI budget stretti (~50% e ~25% del parco slot) hit-rate della policy tier+AUTOPIN+prefetch > hit-rate LRU pura, misurati sull'harness routing (path sync) con JSON committati per entrambe le policy a entrambi i budget; delta riportato e confrontato col ceiling Belady WP-0 (9-19pp in scarsità); pin mai > 12.5% degli slot (assert nel path); suite + tsc puliti | none | src/engine/{residency,expertstore,page}.ts, src/engine/glmroute harness, tests/, results/engine/ | blocked-by-4 |
| 6 | Modello di banda | formula tok/s = f(hit-rate, banda fredda browser, ctx, budget slab) committata CON TEST in npm test; predice i punti MISURATI entro ±15% su ≥ 3 budget slab distinti (12 GiB, ~50%, ~25% del parco — bench committati); la stessa formula predice il TTFT a freddo entro ±15% (verificabile in fase 7); JSON di confronto predetto-vs-misurato committato | none | tools/oracle-moe/sim/ o src/engine/ (modello), tests/, results/engine/ | blocked-by-5 |
| 7 | Instant-on | TTFT a freddo (OPFS popolata, VRAM vuota; router + shared + caldi subito, resto on-demand) ≤ il budget fissato dal ruling docket item 1 (default contrattuale [ASSUMED 1.25×] il TTFT a caldo di riferimento C3b), JSON committato con gap dal budget UX 4 s esplicito; predizione del modello di banda (fase 6) confrontata nel JSON | none | src/engine/{glmmodel,residency,slabfile}.ts, scripts/, results/engine/ | blocked-by-6 + ruling item 1 |
| 8 | Gate floor + non-regressione a perimetro pieno | decode alla config di budget migliore disponibile, quiescente: ≥ 13.43 PASS oppure FAIL dichiarato sotto la clausola pre-negoziata del contratto (misura + attribuzione hardware/clock nel journal, leva residua a docket); gap UX in ogni report; blocco non-regressione completo con run fresche committate (decode/prefill/TTFT/Qwen in banda ±5%; cpuref 256/256 + 512/512; golden ≥ 98.828% b11; routing = firma 14b; ktest PASS; suite ≥ 338+7; tsc pulito); albero congelato durante le run | none | results/ (sole run; nessun edit src durante le run) | blocked-by-7 |
| 9 | Chiusura | checklist DONE WHEN del contratto punto per punto nel journal con PASS/FAIL e artefatto per riga; docket aggiornato; direction §7 fase C CHIUSA; ledger §A; HANDOFF refresh; input hero-demo M4 preparati nel docket (resta PI-gated per hardware); digest; push da goal verificato | none | docs/engine/, HANDOFF.md, .harness/goals/engine-fase-c3c/ | blocked-by-8 |

Note di sizing: fase 1 = 1-2 iterazioni (tool nuovo + protocollo di freddezza);
fase 2 = 1; fase 3 = 1-2; fasi 4-5 = 2-3 l'una (il meccanismo nuovo vive qui);
fase 6 = 1-2; fase 7 = 1-2; fase 8 = 1 (run lunghe, GPU esclusiva, 60 s fra
run); fase 9 = 1. Landmine ereditate: c3c docket item 2 (i-v).
