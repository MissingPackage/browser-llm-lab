# PHASES — engine-fase-c3b (decode ottimistico + repair)

Decomposizione dal contratto v2 (2026-08-07). Sequenziale: le fasi 2-4 toccano
tutte `src/engine/glmmodel.ts` e la telemetria — nessun parallel-group.
Riferimenti di meccanismo: WP-0 (journal c3a it.20), select:"gpu" (it.17),
telemetria a schema unico (it.22).

| # | phase | done-when (mechanical) | authority delta | owns | status |
|---|-------|------------------------|-----------------|------|--------|
| 1 | Spec decode ottimistico | `docs/superpowers/specs/2026-08-07-engine-fase-c3b-decode-ottimistico.md` esiste e fissa TUTTI i punti elencati nel contratto (flag miss piggyback, checkpoint hidden 376 KB, replay dal primo layer sporco, inserimento slotTable differito al confine, prefill sincrono invariato, criterio di rifiuto precondizione, esclusioni: no predictor di confine / no repair batched); registrazione a docket (item nuovo) con nota se tocca gate/soglie (se le tocca: STOP, ruling PI) | none | docs/superpowers/specs/ | ready |
| 2 | Flag di miss + checkpoint hidden (meccanismo, in ktest) | ktest nuovi PASS su GPU reale: (a) flag di miss letto nel readback logits = esattamente gli expert forzati non-residenti (0 falsi positivi su run pulita); (b) buffer checkpoint hidden per layer bit-identico al hidden di riferimento del forward sincrono; ktest esistenti tutti PASS (>= 65); `npm test` senza regressioni; `npx tsc --noEmit` pulito | none | src/engine/glmmodel.ts, src/engine/kernels/, src/engine/telemetry.ts, src/engine/ktest/, tests/ | blocked-by-1 |
| 3 | Repair: replay + slotTable differita + rifiuto | test di identita' del contratto verde su GPU reale: decode ottimistico vs sincrono, argmax identico su tutte le posizioni ([ASSUMED >= 64 token, ctx ~500]), incluso >= 1 caso con miss FORZATO che attiva il replay dal primo layer sporco e 1 caso di rifiuto della precondizione con errore esplicito; inserimenti slotTable SOLO al confine di token (assert nel path); suite + tsc puliti | none | src/engine/{residency,expertstore,page,decodebatch}.ts, src/engine/glmmodel.ts, tests/ | blocked-by-2 |
| 4 | Produzione: glmbench sul ramo ottimistico + gate strutturale | run glmbench quiescente (hostState nel JSON) con report committato in results/engine/ che riporta sync/token <= 2 E submits/token <= 2 (media steady-state ramo decode), P(dirty), miss/token, tassa ms/token, decode/prefill/TTFT + confronto floor C1 + gap UX (assenza = FAIL checklist); confronto dichiarato coi 46 sync/token di C3a | none | src/engine/glmbench/, scripts/glm-bench-run.mjs, results/engine/ | blocked-by-3 |
| 5 | Tassa di replay vs proiezione WP-0 | JSON di confronto committato in results/engine/: P(dirty), miss/token, frazione layer ripetuti, ms/token di tassa vs wp0-replay-sim-2026-08-06.json ALLO STESSO budget slot; entro [ASSUMED +/-25%] = modello confermato, oltre = spiegazione nel journal (parte del done-when) | none | results/engine/, scripts/, tools/oracle-moe/sim/ (sola lettura del sim) | blocked-by-4 |
| 6 | Non-regressione a perimetro pieno (albero congelato) | tutte le righe del blocco non-regressione del contratto verificate con run fresche e JSON/log committati: decode >= 5.211 / prefill >= 25.78 / TTFT <= 17.88 / Qwen >= 326.2 (banda provvisoria item 3: -2% o significativita' su repliche), cpuref 256/256 (gateCpuref JSON) + 512/512 fase A, golden >= 98.828% full-corpus (b11), routing full-corpus = firma 14b, ktest tutti PASS, `npm test` >= 337+7, `npx tsc --noEmit` pulito | none | results/ (sole run; nessun edit src durante le run) | blocked-by-4,5 |
| 7 | Chiusura | checklist DONE WHEN del contratto punto per punto nel journal con PASS/FAIL e artefatto per riga; docket aggiornato; direction §7 riga C3b coi numeri; HANDOFF refresh; input C3c fissati nel docket (pattern c3a item 21: profilo ms/token CON la tassa, residenza, hit-rate LRU ai budget di prova se misurabili, landmine); digest; merge+push da goal verificato | none | docs/engine/direction.md, HANDOFF.md, .harness/goals/engine-fase-c3b/, .harness/goals/engine-fase-c3c/docket.md | blocked-by-6 |

Note di sizing: fase 1 = 1 iterazione; fasi 2-3 = 2-3 iterazioni l'una (il
meccanismo nuovo vive qui); fase 4 = 1-2; fasi 5-6 = 1 l'una (run lunghe, GPU
esclusiva, ~60 s tra run consecutive); fase 7 = 1.
